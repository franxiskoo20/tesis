import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0070ba",
    },
    background: {
      default: "#F5F5F5",
    },
  },
});

export default theme;
